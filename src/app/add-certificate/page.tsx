import CreateCertificate from "@/components/CreateCertificate";
import React from "react";

const AddCertificate = () => {
  return (
    <section id="Add Certificate PAge">
      <CreateCertificate />
    </section>
  );
};

export default AddCertificate;
